import { CommissionRatePipe } from './commission-rate.pipe';

describe('CommissionRatePipe', () => {
  it('create an instance', () => {
    const pipe = new CommissionRatePipe();
    expect(pipe).toBeTruthy();
  });
});
